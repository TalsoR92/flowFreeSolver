class Colored:
    def __init__(self, prev_case, next_case, color):
        self.next_case = next_case
        self.prev_case = prev_case
        
        self.color = color

    def __str__(self):
        return f"Colored: (prev_case={self.prev_case}, next_case={self.next_case}, color={self.color})"

        self.is_point = True

        self.next_case = next_case
        self.prev_case = prev_case
        
        self.color = color

        self.pos = position
        self.connected = connected

        self.pair = pair